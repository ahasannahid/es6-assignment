function doubleNumbers(numbers) {
    return numbers.map(number => number * 2);
  }
  
  
  const inputNumbers = [1, 2, 3, 4, 5];
  const doubledArray = doubleNumbers(inputNumbers);
  console.log(doubledArray);
  